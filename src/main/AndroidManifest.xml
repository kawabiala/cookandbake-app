<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.pingwinek.jens.cookandbake">

    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.USE_CREDENTIALS"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_cookandbake_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_cookandbake_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/CookAndBakeAppTheme"
            android:usesCleartextTraffic="true">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.RegisterActivity"></activity>
        <activity android:name=".activities.LoginActivity"></activity>
        <activity android:name=".activities.RecipeListingActivity"></activity>
        <activity android:name=".activities.RecipeActivity"
                    android:parentActivityName=".activities.RecipeListingActivity"></activity>
    </application>

</manifest>